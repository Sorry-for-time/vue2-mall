import Vue from "vue";
import vuex from "vuex";

Vue.use(vuex);

const state = {};
const actions = {};
const mutations = {};
const getters = {};

export const store = new vuex.Store({
  state,
  actions,
  mutations,
  getters,
});
